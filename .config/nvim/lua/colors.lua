local onedark = require('onedark')
-- onedark theme config
onedark.setup(
  {
    style='deep',
    transparent=true,
    ending_tildes=false,
    code_style = {
      comments = 'italic',
      keywords = 'none',
      functions = 'none',
      strings = 'none', 
      variables = 'none'
    }
  }
)

onedark.load()

--colorscheme catppuccino
--colorscheme catppuccino
--colorscheme onedark
--colorscheme gruvbox
